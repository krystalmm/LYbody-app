$('#chat-wrap').html("<%= j(render 'public/rooms/chat-content', chats: @chats, room: @room) %>");
var textarea = $('#chat-textarea');
textarea.val("");
var scrollHeight = document.getElementById("chat-window").scrollHeight;
document.getElementById("chat-window").scrollTop = scrollHeight;
textarea.height(22);